import { ReactNode } from "react";
interface ThemeProps {
    children?: ReactNode;
}
export default function ThemeProvider({ children }: ThemeProps): import("react/jsx-runtime").JSX.Element;
export {};
